import 'package:flutter/material.dart';

const personalIdInputDecoration = InputDecoration(
  hintText: "Persönliche ID",
  hintStyle: TextStyle(fontSize: 16.0),
);

const passwordInputDecoration = InputDecoration(
  hintText: "Passwort",
  hintStyle: TextStyle(fontSize: 16.0),
);

