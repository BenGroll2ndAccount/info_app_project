
// Preset decorations to insert evenly and easily everywhere needed

import 'package:flutter/material.dart';

// Decoration for ID-Formfield
const personalIdInputDecoration = InputDecoration(
  hintText: "Persönliche ID",
  hintStyle: TextStyle(fontSize: 16.0),
);

//Decoration for Password-Formfield
const passwordInputDecoration = InputDecoration(
  hintText: "Passwort",
  hintStyle: TextStyle(fontSize: 16.0),
);

const newCourseMessageInputDecoration = InputDecoration(
  hintText: "Inhalt der Nachricht",
  hintStyle: TextStyle(fontSize: 14.0),
)